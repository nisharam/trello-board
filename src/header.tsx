import React from "react";
import { StyledHeader } from "./StyledHeader";

const Header  = () => {
    return (
        <StyledHeader>
            <div>
                Trello Clone
            </div>
        </StyledHeader>
    )
}

export default Header;